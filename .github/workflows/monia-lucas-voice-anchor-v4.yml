name: MonIA Lucas Fixed Voice Anchor V4

on:
  push:
    paths:
      - 'scripts/monia_lucas_voice_anchor_v4_energy.py'
      - '.github/workflows/monia-lucas-voice-anchor-v4.yml'
  workflow_dispatch:

jobs:
  generate-anchor-v4:
    runs-on: ubuntu-24.04
    env:
      INFOMANIAK_FTP_HOST: ${{ secrets.INFOMANIAK_FTP_HOST }}
      INFOMANIAK_FTP_USER: ${{ secrets.INFOMANIAK_FTP_USER }}
      INFOMANIAK_FTP_PASSWORD: ${{ secrets.INFOMANIAK_FTP_PASSWORD }}
      HF_TOKEN: ${{ secrets.HF_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python -m pip install --disable-pip-version-check --upgrade pip
          python -m pip install --disable-pip-version-check gradio_client requests pillow numpy
      - name: Generate one energetic Lucas anchor V4 candidate
        run: python -m scripts.monia_lucas_voice_anchor_v4_energy --publish-candidate
      - name: Verify public anchor V4 candidate
        run: |
          python - <<'PY'
          import requests
          url='https://marion-lucas.marionbolomey.fr/resources/monia/generated/lucas-voice-anchor-v4-energy-fr-candidate.wav'
          r=requests.get(url,timeout=60,headers={'Cache-Control':'no-cache'})
          r.raise_for_status()
          assert len(r.content)>4096,len(r.content)
          assert r.content[:4]==b'RIFF',r.content[:12]
          print('verified',url,len(r.content))
          PY
      - name: Candidate-only policy
        run: |
          echo "Single energetic synthetic Lucas V4 anchor candidate."
          echo "Reference video informed broad acoustic/performance traits only; no biometric cloning."
          echo "No visio/runtime mutation and no auto-promotion."
