name: MonIA Lucas Surprise Visio V4 FrontCam

on:
  push:
    branches: [main]
    paths:
      - 'scripts/monia_visio_surprise_v2_pack.py'
      - '.github/workflows/monia-visio-surprise-v2.yml'
  workflow_dispatch:

jobs:
  generate-visio-pack:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      INFOMANIAK_FTP_HOST: ${{ secrets.INFOMANIAK_FTP_HOST }}
      INFOMANIAK_FTP_USER: ${{ secrets.INFOMANIAK_FTP_USER }}
      INFOMANIAK_FTP_PASSWORD: ${{ secrets.INFOMANIAK_FTP_PASSWORD }}
      HF_TOKEN: ${{ secrets.HF_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python -m pip install --disable-pip-version-check --upgrade pip
          python -m pip install --disable-pip-version-check gradio_client requests pillow numpy
          sudo apt-get update -qq
          sudo apt-get install -y -qq ffmpeg
      - name: Generate true front-camera MonIA three-state visio V4
        run: python -m scripts.monia_visio_surprise_v2_pack --state all --publish-candidate
      - name: Verify public front-camera V4 candidate set
        run: |
          python - <<'PY'
          import requests
          base='https://marion-lucas.marionbolomey.fr/resources/monia/generated/'
          files=[
            'visio-lucas-surprise-v4-frontcam-listening-candidate.mp4',
            'visio-lucas-surprise-v4-frontcam-speaking-v4voice-candidate.mp4',
            'visio-lucas-surprise-v4-frontcam-reaction-candidate.mp4',
          ]
          for name in files:
              url=base+name
              r=requests.get(url,timeout=90,headers={'Cache-Control':'no-cache'})
              r.raise_for_status()
              assert len(r.content)>10000,(name,len(r.content))
              assert b'ftyp' in r.content[:64],(name,r.content[:64])
              print('verified',url,len(r.content))
          PY
      - name: Candidate-only policy
        run: |
          echo "MonIA-only true front-camera visio V4 candidate set."
          echo "The viewpoint itself is Lucas's smartphone front camera; no phone may appear in frame."
          echo "No official runtime mutation and no auto-promotion."
          echo "V4 voice remains native-speed during speaking state."
