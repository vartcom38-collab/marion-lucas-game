export type MonIAMemory={id:string;kind:'action'|'dialogue'|'event'|'promise';text:string;day:number;time:string;actors:string[];createdAt:number};
export interface MonIAStorage{put(memory:MonIAMemory):Promise<void>;recent(limit:number):Promise<MonIAMemory[]>;clear():Promise<void>}
class IndexedDBStorage implements MonIAStorage{private dbPromise:Promise<IDBDatabase>|null=null;private db(){if(this.dbPromise)return this.dbPromise;this.dbPromise=new Promise((resolve,reject)=>{const req=indexedDB.open('monia-runtime',1);req.onupgradeneeded=()=>{const db=req.result;if(!db.objectStoreNames.contains('memories'))db.createObjectStore('memories',{keyPath:'id'})};req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error)});return this.dbPromise}async put(memory:MonIAMemory){const db=await this.db();await new Promise<void>((resolve,reject)=>{const tx=db.transaction('memories','readwrite');tx.objectStore('memories').put(memory);tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error)})}async recent(limit:number){const db=await this.db();return new Promise((resolve,reject)=>{const tx=db.transaction('memories','readonly');const req=tx.objectStore('memories').getAll();req.onsuccess=()=>resolve((req.result as MonIAMemory[]).sort((a,b)=>b.createdAt-a.createdAt).slice(0,limit));req.onerror=()=>reject(req.error)})}async clear(){const db=await this.db();await new Promise<void>((resolve,reject)=>{const tx=db.transaction('memories','readwrite');tx.objectStore('memories').clear();tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error)})}}
export const moniaStorage:MonIAStorage=new IndexedDBStorage();
