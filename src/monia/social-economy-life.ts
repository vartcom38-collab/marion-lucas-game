const SAVE_KEY='marion-lucas-save-v4';
type Save={day?:number;money?:number;official?:boolean;married?:boolean;visibility?:number;careerLevel?:number;calendar?:Array<{day?:number;owner?:string;title?:string;note?:string}>;flags?:Record<string,unknown>};
export type SocialEconomySnapshot={householdStage:'separate'|'shared'|'established';budgetBand:'tight'|'comfortable'|'affluent'|'variable';opportunities:Array<{id:string;label:string;kind:'social'|'travel'|'property'|'event';eligible:boolean;reason:string}>;realToreroCirclePolicy:{enabled:true;exclude:string[];note:string};};
function read():Save|null{try{const raw=localStorage.getItem(SAVE_KEY);return raw?JSON.parse(raw) as Save:null}catch{return null}}
function n(v:unknown,f=0){const x=Number(v);return Number.isFinite(x)?x:f}
export function getSocialEconomySnapshot():SocialEconomySnapshot|null{const s=read();if(!s)return null;const f=s.flags||{};const career=n(s.careerLevel,1),visibility=n(s.visibility),shared=!!f.spainHome||!!f.sharedKey||!!s.married;const householdStage=!shared?'separate':s.married?'established':'shared';const budgetBand=career>=5?'affluent':career>=3?'comfortable':career<=1?'tight':'variable';return{householdStage,budgetBand,opportunities:[{id:'taurine-invitations',label:'Invitations du milieu taurin',kind:'event',eligible:!!s.official&&visibility>=8,reason:'Galas, dîners, ferias, remises de prix ou invitations peuvent rejoindre l’agenda de Marion.'},{id:'partner-social-circle',label:'Créer son propre cercle social',kind:'social',eligible:!!s.official,reason:'Marion peut se faire des amis et connaissances sans dépendre uniquement de Lucas.'},{id:'weekend-away',label:'Partir quelques jours',kind:'travel',eligible:budgetBand!=='tight'||career>=2,reason:'Week-end, plage ou voyage court selon calendrier et budget.'},{id:'holiday-property',label:'Projet de résidence de vacances',kind:'property',eligible:householdStage==='established'&&career>=4,reason:'Un achat de résidence secondaire peut devenir un projet long et non automatique.'}],realToreroCirclePolicy:{enabled:true,exclude:['El Rulli'],note:'Les connaissances taurines peuvent utiliser des toreros réels contemporains comme entourage contextuel, jamais comme intrigue imposée. El Rulli est explicitement exclu.'}};}
declare global{interface Window{__moniaSocialEconomyLife?:()=>SocialEconomySnapshot|null}}
window.__moniaSocialEconomyLife=getSocialEconomySnapshot;